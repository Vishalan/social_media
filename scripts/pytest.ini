[pytest]
asyncio_mode = auto
# Ensure `scripts/` is on sys.path regardless of invocation, so
# scripts/broll_gen/__init__.py's bare `from broll_gen.base import …` imports
# resolve and tests' dual-import fallback is deterministic.
pythonpath = .

