# chatterbox-tts pulls gradio transitively which wants fastapi>=0.115.2,
# so the fastapi floor here matches that constraint.
chatterbox-tts==0.1.7
# chatterbox-tts upgrades torch to 2.6 but the pytorch base image's
# torchvision stays at 0.18.1 (built for torch 2.3.x), which makes the
# `torchvision::nms` op silently miss from the dispatcher and blocks
# `from transformers import LlamaModel`. Pin torchvision to the 0.21.x
# line that matches torch 2.6.x.
torchvision==0.21.0
# sentencepiece is a soft dep of transformers' Llama tokenizer path that
# pip doesn't always pull; explicit floor keeps us safe.
sentencepiece>=0.2.0
fastapi>=0.115.2,<1.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.4,<3.0
python-multipart>=0.0.9
